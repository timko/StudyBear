%h1 Discussions#new

=form_tag discussions_path do
  =label :discussion, :statement, "Statement"
  =text_field :discussion, 'statement'
  - if not params[:parent].nil?
    =hidden_field :discussion, 'parent', :value=>params[:parent] 
  =submit_tag "Create"
